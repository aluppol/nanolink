# Secret names only. The server generates each value once and CI makes throwaway ones; never write a value here.
MONGO_INITDB_ROOT_PASSWORD=__GENERATE__
DB_CRUD_PASS=__GENERATE__
DB_REDIRECT_PASS=__GENERATE__
