# Secret names only. The server generates each value once into /srv/apps/nanolink/.env (mode 600);
# CI generates throwaway ones. Non-secret settings live in compose.yaml `environment:`.
MONGO_INITDB_ROOT_PASSWORD=__GENERATE__
MONGO_GATEWAY_PASSWORD=__GENERATE__
MONGO_CREATOR_PASSWORD=__GENERATE__
MONGO_REDIRECT_PASSWORD=__GENERATE__
NATS_PASSWORD=__GENERATE__
VALKEY_PASSWORD=__GENERATE__
DEMO_RESET_TOKEN=__GENERATE__
