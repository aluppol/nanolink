{
  "name": "nanolink-web",
  "version": "1.0.0",
  "private": true,
  "description": "NanoLink entry service: the browser UI and the nginx router in front of the API gateway, the notifier and the redirect service",
  "license": "AGPL-3.0-only",
  "author": "Albert Luppol",
  "type": "module",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "build": "tsx scripts/build.ts",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "lint": "biome check .",
    "format": "biome check --write .",
    "test": "tsx --test test/*.test.ts",
    "check:comments": "tsx scripts/checkComments.ts src test scripts",
    "check": "npm run lint && npm run typecheck && npm run check:comments && npm run test"
  },
  "devDependencies": {
    "@biomejs/biome": "2.5.14",
    "@types/node": "24.13.6",
    "esbuild": "0.28.2",
    "tsx": "4.23.15",
    "typescript": "7.0.2"
  }
}
